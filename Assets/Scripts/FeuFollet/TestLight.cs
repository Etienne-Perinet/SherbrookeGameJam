using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Experimental.Rendering.Universal;

public class TestLight : MonoBehaviour
{
    //public UnityEngine.Experimental.Rendering.Universal.Light2D shapeLight;

    // Start is called before the first frame update
    void Start()
    {
        //shapeLight = GetComponent<UnityEngine.Experimental.Rendering.Universal.Light2D>();
        //shapeLight.pointLightOuterRadius = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
